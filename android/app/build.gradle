plugins {
    id "com.android.application"
}

android {
    namespace "com.anykaraoke.capture"
    compileSdk 35

    defaultConfig {
        applicationId "com.anykaraoke.capture"
        minSdk 29
        targetSdk 35
        versionCode 1
        versionName "0.1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            signingConfig signingConfigs.debug
        }
    }
}
