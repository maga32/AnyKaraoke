# AnyKaraoke 구현 요구사항

이 문서는 이 저장소에서 작업하는 에이전트가 따라야 할 최초 제품 요구사항이다. 사용자가 이후 요구사항을 추가하거나 변경하면, 구현과 함께 이 문서도 최신 상태로 갱신한다.

## 1. 제품 목표

AnyKaraoke는 TV나 큰 화면에서 사용하는 노래방 메인 화면과, 휴대폰에서 사용하는 리모컨 화면으로 구성된 정적 웹사이트다.

- 별도 애플리케이션 서버 없이 HTML, CSS, JavaScript로 구현한다.
- Cloudflare Pages(`pages.dev`)에 정적 배포할 수 있어야 한다.
- 실시간 메시지 교환에는 `https://dweet.cc/`를 사용한다.
- 노래 영상은 YouTube 영상을 embed하여 재생한다.
- 메인 화면이 재생목록과 재생 상태의 최종 권위(authoritative source)다. 리모컨은 명령을 보내고, 메인은 변경된 곡 목록을 모든 리모컨에 전송한다.
- 데스크톱/TV, 태블릿, 휴대폰에서 사용할 수 있도록 반응형으로 구현한다.

프레임워크나 빌드 도구는 꼭 필요할 때만 도입한다. 단순 정적 파일만으로 요구사항을 만족할 수 있다면 그 구성을 우선한다. 비밀 키가 필요한 기능을 클라이언트 코드에 넣지 않는다.

## 2. 페이지와 URL

### 메인 화면

- 경로: `/` 또는 `/index.html`
- 방 파라미터 없이 진입하면 암호학적으로 안전한 난수로 새 방 ID를 생성한다.
- 방 ID는 URL과 dweet 이름에 안전한 URL-safe Base64 문자열이어야 한다(`+`, `/`, `=` 제외).
- 생성한 방을 dweet EventSource로 즉시 구독한다.
- 사용자에게 `/remote?room=${encodeURIComponent(room)}` 형식의 리모컨 URL과 그 URL의 QR 코드를 제공한다.
- 새로고침 시 방을 유지할지 새 방을 만들지는 아래 미결정 사항이 확정되기 전까지, 현재 메인 URL에 `room`이 있으면 재사용하고 없으면 생성한 뒤 `history.replaceState`로 `?room=...`을 기록하는 방향을 기본안으로 삼는다.

구독 형식:

```js
const es = new EventSource(
  `https://dweet.cc/listen/for/dweets/from/${encodeURIComponent(room)}`
);

es.onmessage = event => {
  const dweet = JSON.parse(event.data);
  // 메시지 검증 및 처리
};
```

### 리모컨 화면

- 경로: `/remote` 또는 `/remote/index.html`
- `room` 쿼리 파라미터가 필수다.
- 정상적인 방 ID가 있으면 메인과 동일한 방의 dweet EventSource를 구독한다.
- `room`이 없거나 유효하지 않으면 조작 UI 대신 이해하기 쉬운 오류와 메인 화면으로 돌아가는 방법을 표시한다.
- 접속 직후 메인에 곡 목록을 요청하고, 메인이 보낸 목록을 받아 화면을 구성한다.

Cloudflare Pages에서 `/remote`와 `/remote/`가 동작하도록 실제 `/remote/index.html`을 둔다.

## 3. 메인 화면 UI와 동작

메인은 실제 노래방 기기처럼 전체 화면 사용을 우선한다.

### 기본 모드

- 화면 상단에 현재 곡 제목을 표시한다.
- 그 아래에 다음 곡 제목을 표시한다. 예약곡이 없으면 빈 상태를 명확히 표시한다.
- 나머지 영역은 YouTube 플레이어가 가능한 한 빈틈없이 채운다.
- 영상 비율을 유지하되 화면의 핵심 영역을 최대한 크게 쓴다.
- 브라우저 전체화면 진입이 필요하면 명시적인 사용자 동작으로 실행할 수 있는 컨트롤을 제공한다. 브라우저의 자동재생/전체화면 제한을 고려한다.

### 목록 패널 모드

리모컨에서 `목록 보기` 명령을 보내면:

- 기존 상단 정보와 YouTube 플레이어 영역이 반응형으로 왼쪽으로 밀리며 축소된다.
- 오른쪽에 사이드 패널이 나타난다.
- 패널에는 현재 곡 제목, 현재 키 값, 예약곡 목록, 리모컨 QR 코드, 리모컨 URL을 표시한다.
- 예약곡 목록은 실제 패널 높이에 들어가는 행 수만 노출한다.
- 긴 곡 제목은 잘라 숨기지 말고 흐르는 글씨(접근 가능한 marquee 대체 CSS 애니메이션)로 읽을 수 있게 한다. 모션 감소 설정에서는 애니메이션을 줄이거나 정지한다.

리모컨에서 `목록 닫기` 명령을 보내면 기본 모드로 되돌아간다. 좁은 화면에서는 좌우 레이아웃이 읽기 어려우면 패널을 오버레이 또는 세로 배치로 전환할 수 있으나, 플레이어와 목록 모두 조작 가능해야 한다.

### 재생과 예약목록

- 메인은 YouTube IFrame Player API로 재생, 일시정지, 5초 뒤로 이동, 영상 종료를 제어/감지한다.
- 현재 곡이 정상 종료되면 예약목록의 첫 곡을 현재 곡으로 승격해 자동 재생한다.
- 다음 곡이 없으면 플레이어를 종료/대기 상태로 전환한다.
- `취소`는 현재 곡을 종료하고 다음 예약곡을 현재 곡(목록 인덱스 0)으로 옮기되, 새 현재 곡은 자동재생하지 않고 정지 또는 일시정지 상태로 둔다.
- 반대로 영상이 취소되지 않고 끝까지 정상 재생되어 종료되면 다음 예약곡으로 넘어가 즉시 자동재생한다.
- 일반 예약은 목록 끝에 추가한다.
- 우선 예약은 현재 재생 중인 곡을 방해하지 않고 항상 목록 인덱스 1에 삽입한다. 따라서 더 늦게 들어온 우선 예약이 앞서 들어온 우선 예약보다 먼저 재생되는 LIFO 순서다.
- 예약 취소는 안정적인 곡 ID로 대상을 지정하며, 배열 인덱스나 제목으로 식별하지 않는다.
- 현재 곡을 포함한 모든 곡은 하나의 배열로 관리한다. 인덱스 0은 현재 곡이고 인덱스 1부터 예약곡이다.
- 곡 추가, 예약 취소, 현재 곡 변경처럼 목록이 바뀔 때마다 메인은 전체 최신 곡 배열을 송신한다.

### 키 조정

- 리모컨에서 올림/내림/초기화 명령을 보낼 수 있고 메인은 현재 키 값을 정수로 관리한다.
- 현재 키 값은 메인만 관리하고 메인 목록 패널에 표시한다. 리모컨으로는 키 값을 전송하거나 동기화하지 않는다.
- YouTube 자체에는 노래방식 음정 변경 기능이 없으므로 실제 오디오 피치 변환 구현 여부가 확정되기 전까지 키 값은 상태/UI로 관리한다. 실제로 음정을 바꾸는 것처럼 오해시키는 구현이나 문구를 넣지 않는다.

## 4. 리모컨 UI와 동작

리모컨은 한 손으로도 누르기 쉬운 모바일 우선 노래방 리모컨이어야 한다.

- 연결 중, 연결됨, 연결 끊김/재연결 상태를 표시한다.
- 메인이 보낸 곡 배열을 표시한다. 배열의 0번은 현재 곡이고 그 뒤는 예약곡이다.
- 현재 곡 제어: 키 올림, 키 내림, 키 초기화, 재생, 일시정지, 현재 곡 취소, 5초 뒤로.
- 메인 표시 제어: 목록 보기, 목록 닫기.
- 곡 예약: 사용자가 YouTube URL을 입력하면 video ID를 파싱하고 정규화된 영상 URL, 영상 제목, 우선 예약 여부를 담아 전송한다.
- 일반 예약과 우선 예약을 명확히 구분한다.
- 예약곡별 예약 취소 버튼을 제공한다.
- 잘못된/지원하지 않는 YouTube URL, 비어 있는 입력, 중복 제출, 메인 미접속 등의 실패를 사용자에게 명확히 알린다.
- 곡 목록을 바꾸는 명령은 송신 성공만으로 화면에 확정하지 않는다. 메인이 다시 보낸 곡 배열을 반영한다. 재생 상태와 키 값은 리모컨에 동기화하지 않는다.

지원할 YouTube URL 파서는 최소한 다음 형식을 처리한다.

- `https://www.youtube.com/watch?v=VIDEO_ID`
- `https://youtu.be/VIDEO_ID`
- `https://www.youtube.com/shorts/VIDEO_ID`
- `https://www.youtube.com/embed/VIDEO_ID`
- 모바일 서브도메인 및 위 URL에 추가 쿼리 파라미터가 붙은 경우

영상 제목 조회는 브라우저에서 비밀 API 키 없이 가능한 방법을 사용해야 한다. 조회가 실패했을 때 예약 자체를 막을지, 사용자가 제목을 직접 입력하게 할지는 아래 미결정 사항이 확정되기 전까지 직접 제목 입력 fallback을 제공하는 방향을 기본안으로 삼는다.

## 5. dweet 메시지 인터페이스

메인과 리모컨은 같은 방 주소를 사용하므로 모든 애플리케이션 메시지에 발신 역할과 메시지 종류를 넣는다. dweet 응답의 실제 payload 위치가 래퍼 내부라면 먼저 앱 payload를 안전하게 추출한 뒤 아래 스키마를 검증한다.

권장 공통 envelope:

```js
{
  source: "main" | "remote",
  type: "list" | "command" | "request_list",
  payload: {}
}
```

`type`은 같은 dweet 스트림에서 메시지의 용도를 구분하는 표식이다. `list`는 메인이 보내는 곡 배열, `command`는 리모컨이 보내는 조작, `request_list`는 새 리모컨이 보내는 목록 요청을 뜻한다.

모든 수신자는 다음 메시지를 무시한다.

- 필수 필드나 payload 형식이 잘못된 메시지
- 역할과 type 조합이 맞지 않는 메시지

### 메인 송신

메인은 `source: "main"`, `type: "list"`로 전체 곡 배열을 보낸다. 부분 변경만 보내지 않는다.

목록 payload의 전체 형태:

```js
[
  {
    id: "youtube-video-id",
    url: "https://www.youtube.com/watch?v=...",
    title: "곡 제목"
  }
]
```

- 배열이 비어 있으면 현재 곡과 예약곡이 모두 없는 상태다.
- 배열의 0번이 현재 곡이며, 1번부터 차례대로 예약곡이다.
- 곡 추가/취소/전환으로 배열이 바뀌면 전체 배열을 송신한다.
- 새 리모컨의 `request_list`를 받으면 변경이 없어도 현재 배열을 즉시 재송신한다.

### 메인 수신 / 리모컨 송신

리모컨은 `source: "remote"`, `type: "command"`로 명령을 보낸다. `payload.action`의 최소 목록:

- `play`
- `pause`
- `seek_back`, `{ seconds: 5 }`
- `cancel_current`
- `key_up`
- `key_down`
- `key_reset`
- `open_list`
- `close_list`
- `enqueue`, `{ item: { id, url, title } }`
- `enqueue_priority`, `{ item: { id, url, title } }`
- `cancel_queued`, `{ id: "youtube-video-id" }`

리모컨의 최초 목록 요청은 `source: "remote"`, `type: "request_list"`, 빈 payload로 보낸다.

메인은 명령 payload를 신뢰하지 말고 타입, 문자열 길이, YouTube video ID, 허용 action, 숫자 범위를 검증한 뒤 적용한다. 알 수 없는 명령은 무시한다. 여러 리모컨의 명령은 메인이 수신한 순서대로 적용한다.

### 리모컨 수신

- `source: "main"`, `type: "list"`만 곡 목록으로 적용한다.
- 일정 시간 목록을 받지 못했다고 곧바로 화면을 비우지 않는다. 마지막 목록을 유지하면서 연결 상태를 별도로 표시하고 다시 목록을 요청한다.

## 6. 안정성, 보안, 접근성

- 방 ID를 아는 사람은 제어할 수 있다는 dweet 기반 구조의 한계를 UI/문서에서 숨기지 않는다. 방 ID는 추측하기 어려운 충분한 엔트로피를 사용한다.
- 외부 메시지의 HTML을 `innerHTML`로 삽입하지 않는다. 제목과 URL은 텍스트로 렌더링하고 URL scheme/host/video ID를 검증한다.
- EventSource 오류 시 브라우저 기본 재접속을 활용하되 연결 상태를 표시하고, 재연결 후 상태를 다시 요청한다.
- 송신 버튼은 실수로 연속 입력되지 않도록 필요한 범위에서 짧게 debounce/throttle한다.
- QR 코드 라이브러리를 외부 CDN에서 불러온다면 로드 실패 시에도 텍스트 URL은 항상 남아 있어야 한다.
- YouTube embed와 외부 스크립트가 실패했을 때 빈 화면 대신 오류/재시도 상태를 표시한다.
- 터치 대상 크기, 키보드 조작, focus 표시, 색 대비, `aria-label`을 고려한다.
- 사용자 설정 `prefers-reduced-motion`을 존중한다.

## 7. 상태 유지

상태는 다음과 같이 유지한다.

- 메인이 열려 있는 동안 메모리 상태만 최종 권위로 사용한다.
- `localStorage` 등 브라우저 영구 저장소에 방이나 곡 목록, 재생 상태를 저장하지 않는다.
- 메인 새로고침으로 메모리 상태가 사라지면 목록은 초기화된다.
- 리모컨은 곡 목록을 저장하지 않고 메인에 다시 요청한다.
- 서로 다른 탭/기기에서 같은 방으로 메인을 두 개 띄우는 경우는 지원 대상이 아니다. 이를 감지할 수 있으면 경고한다.

## 8. 구현 구조 지침

구체적인 파일 분리는 구현 과정에서 조정할 수 있지만 역할을 섞지 않는다.

- `/index.html`: 메인 문서
- `/remote/index.html`: 리모컨 문서
- 공용 JavaScript: 방 ID, dweet 연결/송신, 메시지 검증, YouTube URL 파싱, 공용 모델
- 메인 JavaScript: authoritative state, queue, YouTube IFrame API, 곡 목록 broadcast
- 리모컨 JavaScript: 명령 송신, 곡 목록 반영, 모바일 UI
- 공용/페이지별 CSS: 전체화면 메인과 모바일 리모컨 레이아웃

인라인 코드 한 파일에 모든 로직을 누적하지 말고, 테스트 가능한 순수 함수(YouTube URL 파싱, 메시지 검증, queue 연산)를 분리한다. 정적 호스팅에서 경로가 깨지지 않도록 절대 도메인 경로 또는 현재 페이지 기준 경로를 의도적으로 사용한다.

## 9. 검증 및 완료 조건

구현 에이전트는 최소한 다음을 검증한다.

- `/` 진입 시 안전한 방 생성, dweet 구독, 올바른 리모컨 URL/QR 표시
- `/remote?room=...` 진입 시 동일 방 구독 및 상태 요청
- 메인/리모컨/다른 방/자기 메시지 필터링
- 일반 예약, 우선 예약, 예약 취소, 현재 곡 취소와 다음 곡 전환
- 재생, 일시정지, 정확히 5초 뒤로(0초 미만 clamp)
- 영상 종료 시 다음 곡 자동 재생 및 상태 broadcast
- 키 상태 변경/초기화 동기화
- 목록 패널 열기/닫기와 데스크톱/모바일 반응형 표시
- 긴 제목 흐르는 표시 및 reduced-motion 처리
- 메인보다 늦게 접속하거나 재접속한 리모컨의 전체 곡 목록 동기화
- 잘못된 URL, 악성 제목 문자열, 잘못된 dweet payload를 안전하게 거부
- Cloudflare Pages와 일반 정적 파일 서버에서 `/`와 `/remote/`가 동작

가능하면 URL 파서, 메시지 검증, queue 연산은 자동 테스트를 작성한다. 브라우저 통합 검증은 실제 YouTube API와 dweet 네트워크 실패 상황도 포함한다.

## 10. 미결정 사항

다음은 사용자 확인 전 임의로 제품 정책을 굳히지 않는다. 구현을 진행해야 한다면 위에 명시한 기본안을 사용하고, 변경하기 쉽게 격리한다.

1. 메인 새로고침 시 URL의 같은 방 ID는 유지하되 곡 목록은 초기화하는 현재 정책을 변경할 필요가 있는지.
2. 키 조정이 숫자 표시만 필요한지, Web Audio 등을 이용한 실제 음정 변환까지 필요한지. YouTube embed 제약 때문에 후자는 별도 기술 검증이 필요하다.
3. 영상 제목 자동 조회 실패 시 직접 입력을 허용할지, 예약을 거부할지.
4. 같은 영상의 중복 예약을 허용할지. 영상 ID를 곡 ID로 사용하므로 중복을 허용한다면 취소 대상 식별 규칙을 추가해야 한다.
5. 키 값의 허용 범위(예: `-6`~`+6`)와 한 번 누를 때의 단위.
6. 리모컨을 여러 명이 동시에 사용하는 경우 권한 차등이나 진행자 잠금이 필요한지.
